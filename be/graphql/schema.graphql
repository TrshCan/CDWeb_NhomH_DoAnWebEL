# ----------------------------------
# Query (bắt buộc, có thể để trống)
# ----------------------------------
type Query {
  # Lấy toàn bộ sự kiện
  getAllEvents: [Event!]! 
    @field(resolver: "App\\GraphQL\\Resolvers\\EventResolver@getAllEvents")

  # Lấy chi tiết sự kiện theo ID
  getEventById(id: ID!): Event 
    @field(resolver: "App\\GraphQL\\Resolvers\\EventResolver@getEventById")

  _empty: String
}

# ----------------------------------
# Event type
# ----------------------------------
type Event {
  id: ID!
  title: String!
  event_date: String!      # date lưu dạng string
  location: String
  created_by: ID!
  created_at: String
  deleted_at: String
}

# ----------------------------------
# Input types
# ----------------------------------
input CreateEventInput {
  title: String!
  event_date: String!
  location: String
}

input UpdateEventInput {
  title: String
  event_date: String
  location: String
}

# ----------------------------------
# Mutations
# ----------------------------------
type Mutation {
  # 🟢 Tạo event mới
  createEvent(input: CreateEventInput!): Event 
    @field(resolver: "App\\GraphQL\\Resolvers\\EventResolver@createEvent")

  # 🟡 Cập nhật event
  updateEvent(id: ID!, input: UpdateEventInput!): Event
    @field(resolver: "App\\GraphQL\\Resolvers\\EventResolver@updateEvent")

  # 🔴 Xóa event
  deleteEvent(id: ID!): Boolean
    @field(resolver: "App\\GraphQL\\Resolvers\\EventResolver@deleteEvent")
}
